[project]
name = "bt-model-1"
version = "0.1.0"
description = "Binance futures parameter-sweep backtester and Dash dashboard"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dash>=3.0",
    "numpy>=2.0",
    "pandas>=2.2",
    "plotly>=6.0",
    "pyarrow>=19.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false
